const UserProfile = async () => {
  // if (!user) {
  //   return <p>該当のユーザーが見つかりません</p>;
  // }

  return (
    <>
      {/* <p>{user.email}</p>
      <p>{user.name}</p> */}
      <p>テスト</p>
    </>
  );
};
export default UserProfile;
